name: Deploy master
on:
  [create,  push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Dump GitHub context
      env:
        GITHUB_CONTEXT:  $(echo ${{ toJson(github.ref)}} | awk '{print $3}')
        
      run: |
        echo "$GITHUB_CONTEXT"
       
        
 
