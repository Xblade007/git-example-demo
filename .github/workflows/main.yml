name: Deploy master
on:
  [create,  push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Dump GitHub context
      env:
        GITHUB_CONTEXT: ${{ toJson(github).jsonBody["ref"] }}
        
      run: |
        echo "$GITHUB_CONTEXT"
        
